<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.player.user.mapper.LoginLogMapper">

    <insert id="insertLoginLog" parameterType="com.player.user.entity.LoginLogEntity"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO login_log(user_id, ip, login_type, create_time)
        VALUES(#{userId}, #{ip}, #{loginType}, now())
    </insert>

</mapper>
